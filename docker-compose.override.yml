version: "3.8"
services:
  backend:
    environment:
      NODE_ENV: development
    volumes:
      - ./backend:/app/backend
      - backend_node_modules:/app/backend/node_modules
    ports:
      - "9000:9000"

  admin:
    ports:
      - "7000:7000"


volumes:
  backend_node_modules:
